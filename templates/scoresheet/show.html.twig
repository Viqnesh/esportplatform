{% extends 'base.html.twig' %}

{% block title %}Scoresheet{% endblock %}

{% block body %}
    <h1>Scoresheet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scoresheet.id }}</td>
            </tr>
            <tr>
                <th>ScoreEquipeA</th>
                <td>{{ scoresheet.scoreEquipeA }}</td>
            </tr>
            <tr>
                <th>ScoreEquipeB</th>
                <td>{{ scoresheet.scoreEquipeB }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_scoresheet_index') }}">back to list</a>

    <a href="{{ path('app_scoresheet_edit', {'id': scoresheet.id}) }}">edit</a>

    {{ include('scoresheet/_delete_form.html.twig') }}
{% endblock %}
