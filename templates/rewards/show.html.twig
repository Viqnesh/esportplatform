{% extends 'base.html.twig' %}

{% block title %}Rewards{% endblock %}

{% block body %}
    <h1>Rewards</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reward.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ reward.libelle }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ reward.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rewards_index') }}">back to list</a>

    <a href="{{ path('app_rewards_edit', {'id': reward.id}) }}">edit</a>

    {{ include('rewards/_delete_form.html.twig') }}
{% endblock %}
