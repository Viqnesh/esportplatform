{{ form_start(form) }}
  <div class="mb-3 row">
    <label for="staticEmail" class="col-sm-2 col-form-label">Nom</label>
    <div class="col-sm-10">
    {{ form_widget(form.nom) }}
    </div>
  </div>
    <button class="btn btn-primary mt-3">{{ button_label|default('Enregistrez') }}</button>
{{ form_end(form) }}
