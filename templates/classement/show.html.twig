{% extends 'base.html.twig' %}

{% block title %}Classement{% endblock %}

{% block body %}
    <h1>Classement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classement.id }}</td>
            </tr>
            <tr>
                <th>MP</th>
                <td>{{ classement.MP }}</td>
            </tr>
            <tr>
                <th>W</th>
                <td>{{ classement.W }}</td>
            </tr>
            <tr>
                <th>L</th>
                <td>{{ classement.L }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_classement_index') }}">back to list</a>

    <a href="{{ path('app_classement_edit', {'id': classement.id}) }}">edit</a>

    {{ include('classement/_delete_form.html.twig') }}
{% endblock %}
